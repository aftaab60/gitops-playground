apiVersion: kargo.akuity.io/v1alpha1
kind: Project
metadata:
  name: gitops-tracker
  # A Project in Kargo v1 is a namespace wrapper — it creates the gitops-tracker
  # namespace and registers it as a Kargo project. Promotion policies are
  # configured per-Stage via annotations or the Kargo UI.

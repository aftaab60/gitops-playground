apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gitops-staging

resources:
  - namespace.yaml
  - ../base

# Kargo updates newTag here when promoting a new nginx version to staging.
images:
  - name: nginx
    newTag: alpine

patches:
  - path: app-config-patch.yaml
    target:
      kind: ConfigMap
      name: app-config
  - path: api-patch.yaml
    target:
      kind: Deployment
      name: api
  - path: frontend-patch.yaml
    target:
      kind: Deployment
      name: frontend

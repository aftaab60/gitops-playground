apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  # ClusterIP (default) — only reachable within the cluster.
  # The Go API connects to postgres-service:5432 via this service.
